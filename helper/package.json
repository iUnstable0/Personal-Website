{
  "name": "helper",
  "module": "index.ts",
  "scripts": {
    "start": "doppler run -- cross-env ENVIRONMENT=production bun run src/index.ts",
    "staging": "doppler run -- cross-env ENVIRONMENT=staging bun run src/index.ts",
    "dev": "doppler run -- cross-env ENVIRONMENT=development bun run --watch src/index.ts"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "better-logging": "^5.0.0",
    "chalk": "^5.4.1",
    "cross-env": "^7.0.3",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1"
  }
}