// Resolver functions (also contains schema definitions categorized by the resolver)
pub mod handler;

// Schemas that are too broad to be categorized
pub mod shared_schema;

// Ignore this
pub mod mutation;
pub mod query;